<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Foto2.Pages.CommentPage"
             Title="Comments">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Comments List -->
        <CollectionView Grid.Row="0" ItemsSource="{Binding Comments}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout Padding="10">
                        <Label Text="{Binding Username}" FontAttributes="Bold" />
                        <Label Text="{Binding Content}" TextColor="Gray" />
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <!-- Add Comment Section -->
        <StackLayout Grid.Row="1" Orientation="Horizontal" Padding="10">
            <Entry x:Name="CommentEntry" Placeholder="Add a comment..." HorizontalOptions="FillAndExpand" />
            <Button Text="Send" Command="{Binding AddCommentCommand}" CommandParameter="{Binding Source={x:Reference CommentEntry}, Path=Text}" />
        </StackLayout>
    </Grid>
</ContentPage>
